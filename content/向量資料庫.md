---
---

# 向量資料庫

> 儲存和檢索向量嵌入（embeddings）的專用資料庫，是 [[rag|RAG]] 和語意搜尋的基礎設施。

## 什麼是向量資料庫

向量資料庫專門用來儲存高維向量（embeddings），並提供快速的近似最近鄰搜尋（ANN）。在 LLM 應用中，文字先轉成向量，再透過向量資料庫做語意搜尋。

## 核心概念

| 概念 | 說明 |
|------|------|
| Embedding | 將文字/圖片轉成固定維度的向量表示 |
| ANN 搜尋 | 近似最近鄰搜尋，犧牲少量精度換取大幅加速 |
| 索引演算法 | HNSW、IVF、PQ 等加速搜尋的資料結構 |
| 距離度量 | cosine similarity、L2、inner product |

## 主流向量資料庫

| 資料庫 | 特色 | 開源 |
|--------|------|------|
| ChromaDB | Python 友善，輕量 | ✅ |
| Pinecone | 全託管，效能好 | ❌ |
| Weaviate | 支援混合搜尋 | ✅ |
| Qdrant | Rust 實現，高效 | ✅ |
| Milvus | 大規模分散式 | ✅ |
| pgvector | PostgreSQL 擴充 | ✅ |

## 在 RAG 中的角色

```
文件 → 切塊 → Embedding → 存入向量資料庫
                                    ↓
查詢 → Embedding → 語意搜尋 → 取得相關文件 → 餵給 LLM
```

向量資料庫是 [[rag|RAG]] 管線的核心儲存層。

## 與知識庫的關係

我們的知識庫系統（raw→wiki→lint）不完全依賴向量搜尋，而是用結構化的 wiki 頁面 + 概念索引。但向量搜尋可作為補充：
- 在 wiki 頁面內做語意搜尋
- 跨頁面的相關性排序
- [[MemPalace-mempalace|MemPalace]] 用 ChromaDB 做 Drawer 層的語意搜尋

## 相關主題

- [[rag|RAG]] — 檢索增強生成
- [[MemPalace-mempalace|MemPalace]] — 使用 ChromaDB 的記憶系統
- [[Knowledge-Graph|知識圖譜]]